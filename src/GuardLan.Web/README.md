# GuardLAN Web

Angular 20 dashboard client for GuardLAN.

## Development

```bash
npm start
```

The app runs at `http://localhost:4200` and is intended to call the API at `http://localhost:5232`.

## Build

```bash
npm run build
```
